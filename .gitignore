.DS_Store
node_modules/
**/dist/
04-prototype/node_modules/

# Secrets and local env files
.env
.env.*
*.pem
*.key

# ...but DO track example/template env files (no secrets, documents config).
!.env.example
!*.env.example

# Project-specific local env files
05-engineering/apps/api/.env
04-prototype/.env.local

# Vercel local project metadata
**/.vercel/

# Local Claude / Cursor agent project memory (machine-specific paths)
.claude/

# Per-user file-adapter settings written by the API in dev (Supabase is the
# real store; these are local dev artifacts only).
05-engineering/apps/api/data/settings_user_*.json
