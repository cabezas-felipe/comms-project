/**
 * @tempo/web — reserved for the production Vite/React app.
 * Until migration (later slice), run the UI from `04-prototype` via `npm run dev` in `05-engineering/`.
 */
export type { DashboardPayload, SettingsPayload } from "@tempo/contracts";
export type { AnalyticsEvent } from "@tempo/analytics";
