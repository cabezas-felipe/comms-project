-- Migration 001: Initial schema (extracted from schema.sql for slice 11)
-- Run via Supabase SQL editor or CLI: supabase db push
-- See full schema at: src/db/schema.sql

-- settings, stories, summaries tables + RLS — see schema.sql for full DDL.
-- This file marks the baseline for migration tracking introduced in slice 18.
